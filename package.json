{
  "devDependencies": {
    "process": "^0.11.10",
    "typescript": "^3.4.5"
  },
  "dependencies": {
    "axios": "^0.18.0"
  },
  "scripts": {
    "start:db": "npx json-server --watch db.json",
    "start:parcel": "npx parcel index.html"
  }
}
